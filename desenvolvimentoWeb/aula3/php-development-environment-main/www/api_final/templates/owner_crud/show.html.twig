{% extends 'base.html.twig' %}

{% block title %}Owner{% endblock %}

{% block body %}
    <h1>Owner</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ owner.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ owner.name }}</td>
            </tr>
            <tr>
                <th>Nickname</th>
                <td>{{ owner.nickname }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_owner_crud_index') }}">back to list</a>

    <a href="{{ path('app_owner_crud_edit', {'id': owner.id}) }}">edit</a>

    {{ include('owner_crud/_delete_form.html.twig') }}
{% endblock %}
